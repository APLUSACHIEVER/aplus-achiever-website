(()=>{const DATA_URL="./data/a-share-dividend-radar.json",HISTORY_URL="./data/a-share-dividend-history.json";const state={rows:[],filtered:[]};const $=id=>document.getElementById(id);const n=v=>Number.isFinite(Number(v))?Number(v):null;const money=v=>n(v)==null?"—":n(v).toFixed(2);const pct=v=>n(v)==null?"—":n(v).toFixed(2)+"%";const pe=v=>n(v)==null?"—":n(v).toFixed(2);const target=(d,y)=>d>0?d/y:null;const COMPLETED_YEAR=new Date().getFullYear()-1;function histMap(rows){const m=new Map;for(const x of rows||[]){const code=String(x.code||"").trim();const d=n(x.cashDividendPerShare);const y=String(x.reportYear||"").slice(0,4);if(!code||d==null||d<0||!/^[0-9]{4}$/.test(y))continue;if(!m.has(code))m.set(code,new Map);const ym=m.get(code);ym.set(y,(ym.get(y)||0)+d)}return m}function annualSeries(ym){const vals=[];for(let y=COMPLETED_YEAR-4;y<=COMPLETED_YEAR;y++)vals.push({year:y,dividend:Number((ym.get(String(y))||0).toFixed(4))});return vals}function normalise(r,hm){const price=n(r.price),ym=hm.get(String(r.code))||new Map,series=annualSeries(ym),latest=series[series.length-1].dividend,avg=series.reduce((s,x)=>s+x.dividend,0)/series.length,latestYield=price>0?latest/price*100:null;return {...r,completedDividend:latest,normalDividend:avg,normalYield:latestYield,latestYield,historyYears:series.length,historyYearsUsed:series.map(x=>String(x.year)),yield:latestYield,target45:target(latest,.045),target50:target(latest,.05),target60:target(latest,.06)}}async function load(){try{$("marketStatus").textContent="🟡 正在读取全A股及年度分红数据库…";const[ra,rh]=await Promise.all([fetch(DATA_URL+"?t="+Date.now(),{cache:"no-store"}),fetch(HISTORY_URL+"?t="+Date.now(),{cache:"no-store"})]);if(!ra.ok||!rh.ok)throw Error("database unavailable");const rp=await ra.json(),hp=await rh.json();if(!Array.isArray(rp.rows)||!rp.rows.length)throw Error("radar database empty");if(!Array.isArray(hp.rows)||!hp.rows.length)throw Error("history database empty");const hm=histMap(hp.rows);state.rows=rp.rows.map(r=>normalise(r,hm));renderStats(rp);filterRows()}catch(e){console.warn("A-share normalized dividend radar load failed:",e);$("marketStatus").textContent="🔴 年度分红数据库暂时无法读取";$("lastUpdated").textContent="请稍后刷新";$("radarBody").innerHTML="<tr><td colspan='8' class='loading'>年度分红数据库暂时无法连接，请稍后刷新</td></tr>"}}function filterRows(){const q=$("stockSearch").value.trim().toLowerCase(),min=Number($("yieldFilter").value)||0,sort=$("sortBy").value;let rows=state.rows.filter(r=>{const match=!q||String(r.code).includes(q)||String(r.name||"").toLowerCase().includes(q);return match&&(r.yield==null?min===0:r.yield>=min)});rows.sort((a,b)=>{if(sort==="yield")return(b.yield??-1)-(a.yield??-1);if(sort==="price")return(a.price??Infinity)-(b.price??Infinity);if(sort==="change")return(b.changePct??-Infinity)-(a.changePct??-Infinity);if(sort==="code")return String(a.code).localeCompare(String(b.code));return String(a.name).localeCompare(String(b.name),"zh-CN")});const displayRows=q?rows:rows.slice(0,200);state.filtered=displayRows;render(displayRows)}function render(rows){$("resultCount").textContent=$("stockSearch").value.trim()?"搜索结果 "+rows.length+"｜全 A 股搜索":"默认显示即时股息率最高 200 只｜全 A 股覆盖";if(!rows.length){$("radarBody").innerHTML="<tr><td colspan='8' class='empty'>没有找到符合条件的股票</td></tr>";return}$("radarBody").innerHTML=rows.map(r=>{const cls=r.change>0?"up":r.change<0?"down":"";return "<tr><td><span class='stock-name'>"+r.name+"</span><span class='ticker'>"+r.code+"</span></td><td class='price'>"+money(r.price)+"</td><td class='"+cls+"'>"+(r.change==null?"—":money(r.change)+" ("+pct(r.changePct)+")")+"</td><td class='yield'>"+pct(r.yield)+"</td><td>"+pe(r.pe)+"</td><td class='target'>"+(r.target45?money(r.target45):"—")+"</td><td class='target'>"+(r.target50?money(r.target50):"—")+"</td><td class='target'>"+(r.target60?money(r.target60):"—")+"</td></tr>"}).join("")}function renderStats(payload){const rows=state.rows.filter(r=>r.price!=null),valid=rows.filter(r=>r.yield!=null),highest=valid.reduce((a,b)=>!a||b.yield>a.yield?b:a,null),five=valid.filter(r=>r.yield>=5).length,avg=valid.length?valid.reduce((s,r)=>s+r.yield,0)/valid.length:null;$("stats").innerHTML="<div class='stat'><div class='stat-label'>A股覆盖</div><div class='stat-value'>"+state.rows.length.toLocaleString()+"</div><div class='stat-note'>全A股｜最近完整财年实际现金分红</div></div><div class='stat'><div class='stat-label'>最高股息率</div><div class='stat-value'>"+(highest?highest.name+" "+pct(highest.yield):"—")+"</div><div class='stat-note'>2025完整财年实际现金分红/股 ÷ 当前股价</div></div><div class='stat'><div class='stat-label'>≥5%股息率</div><div class='stat-value'>"+five+"</div><div class='stat-note'>即时高股息</div></div><div class='stat'><div class='stat-label'>市场平均</div><div class='stat-value'>"+pct(avg)+"</div><div class='stat-note'>全市场即时股息率简单平均</div></div>";const dt=payload.generatedAt?new Date(payload.generatedAt):null;$("lastUpdated").textContent=dt?"行情数据更新："+dt.toLocaleString("zh-CN",{hour12:false})+"｜股息率按"+COMPLETED_YEAR+"完整财年实际现金分红计算":"数据更新：—";$("marketStatus").textContent=state.rows.length>=1000?"🟢 全A股数据库已就绪｜即时股息率":"🟠 全A股数据库正在刷新"}["stockSearch","yieldFilter","sortBy"].forEach(id=>$(id).addEventListener(id==="stockSearch"?"input":"change",filterRows));load();setInterval(load,60000)})();